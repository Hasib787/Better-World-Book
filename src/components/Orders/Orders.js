import React from 'react';
import { UserContext } from '../../App';

const Orders = () => {
    return (
        <div>
            <h1>This is Order</h1>
        </div>
    );
};

export default Orders;